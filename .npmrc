# Set URL for your scoped packages.
# For example package with name `@foo/bar` will use this URL for download
npm config set @izzius94:registry https://gitlab.com/api/v4/projects/express-request-validator/packages/npm/

# Add the token for the scoped packages URL. Replace <your_project_id>
# with the project where your package is located.
npm config set -- '//gitlab.com/api/v4/projects/express-request-validator/packages/npm/:_authToken' "${NPM_TOKEN}"
npm config set -- '//gitlab.com/api/v4/packages/npm/:_authToken' "${NPM_TOKEN}"
